import { HeadingTag } from '../components/headingTag';
import AppLayout from '../layouts/appLayout';
const Settings = () => {
    return (
        <AppLayout>
            <HeadingTag>Settings</HeadingTag>
        </AppLayout>
    );
};

export default Settings;
